※ 아래 본문을 지원 폼에 그대로 붙여넣으세요.

────────────────────────────────────────

안녕하세요.
공고의 화면을 먼저 만들어 보고, 진행하면 막힐 지점 세 가지를 실제로 확인한 뒤
지원합니다.

  지도 탐색 데모
  https://jeonjaeman.github.io/demo-sites/pindex/

  착수 전 검토 (버튼을 눌러 직접 확인하실 수 있습니다)
  https://jeonjaeman.github.io/demo-sites/pindex/admin/

  매물 2,467건으로 클러스터링·필터·오버레이가 실제로 동작합니다.


[1] 2-3 과 3-2 는 브라우저 단독으로 못 합니다

  "공공데이터 API 연동"과 "안전한 토큰 관리"가 프론트 과업으로 적혀 있습니다.
  브라우저에서 직접 호출해 확인한 결과입니다.

    단순 GET               통과   Access-Control-Allow-Origin: *
    프리플라이트(OPTIONS)  실패   204 응답에 ACAO 헤더 없음
    커스텀 헤더 GET        실패   Failed to fetch
    서비스키               노출   URL 쿼리에 그대로 · 도메인 제한 없음

  흔히 "공공데이터는 CORS가 막힌다"고 하는데 실제로는 단순 GET이면 통과합니다.
  진짜 문제는 키입니다. serviceKey 가 주소에 그대로 실려 개발자도구에 남고,
  공공데이터포털 키에는 카카오 JavaScript 키 같은 도메인 제한이 없어서 복사해
  가면 누구나 씁니다. 일일 한도(기본 1,000회)가 남에게 소진됩니다.

  브라우저에 내려간 값은 전부 보이므로 "안전한 토큰 관리"는 원리적으로
  성립하지 않습니다. 서버 프록시가 유일한 답입니다.

  → 데모 [프론트 경계선]에서 버튼을 누르면 이 페이지가 실제로 공공 API를 호출하고,
    키가 어디에 남는지 그대로 보입니다. 요구사항 7건을 프론트 / 프론트+백엔드 /
    백엔드 필요로 나눈 표도 함께 두었습니다.
    백엔드에 공공 API 프록시 엔드포인트가 있는지가 착수 전 1순위 확인 항목입니다.


[2] 지도는 좌표가 없고, 마커는 1,000개부터 버벅입니다

  국토부 실거래가 API는 법정동코드 5자리 + 계약년월로 조회하고, 응답에는
  지번과 법정동명만 옵니다. GPS 좌표가 없어 그대로는 핀을 찍을 수 없습니다.
  지오코딩이 필요하고 카카오 로컬 API 쿼터(일 10만)를 씁니다.

  마커 성능도 실제로 재봤습니다. 같은 데이터로 프레임 시간을 측정한 값입니다.

    개별 마커 2,371개    33.4ms / 프레임   ← 30fps 미만, 드래그가 끊깁니다
    클러스터링 71개       1.0ms / 프레임

  33배 차이입니다. 공고 3-1 "지도 렌더링 지연 최소화"가 이 지점입니다.

  → 데모 지도 좌하단 [렌더 성능] 패널에서 클러스터링을 꺼 보시면 숫자가 바로
    바뀝니다. 뷰포트 밖은 아예 그리지 않는 컬링도 넣었습니다.
    좌표 매칭에 실패한 96건(3.9%)은 지도에서 실제로 빠지고 목록에 경고가 붙습니다 -
    조용히 사라지면 데이터가 없는 서비스로 보이기 때문입니다.
    실서비스에서는 서버에서 1회 지오코딩 후 좌표를 캐시하는 구조를 권합니다.


[3] 계약 30일과 오픈 희망일 사이에 13일이 비어 있습니다

    착수                       2026-08-03
    4주 마일스톤 (PG 완료)      2026-08-31
    계약 기간 종료 (+30일)      2026-09-02
    최종 오픈 희망일            2026-09-15   ← 계약 기간 밖

  마일스톤 2와 계약 종료 사이가 2일뿐이라 QA·크로스브라우징·배포가 들어갈 자리가
  없습니다. 그 작업이 13일 구간으로 밀리는데 그 구간이 계약에 없습니다.
  적어 두지 않으면 무상 대응 구간이 됩니다.

  → 0차 검토(무상 1~2일)로 기존 코드를 먼저 보고 공수를 확정한 뒤,
    1차 30일(퍼블리싱·지도·목록/필터·라우팅·OAuth) / 2차 1~2주(공공 API·PG·게시판·QA)로
    나누는 안을 제안합니다. 13일을 2차로 명시하면 공백이 사라집니다.

  그 외 - PG 결제는 프론트 콜백 금액을 그대로 믿으면 브라우저에서 0원으로 바꿔도
  통과합니다. 데모 [결제 검증]에서 직접 바꿔 보실 수 있습니다. 서버가 PG 원장을
  재조회해 대조해야 하며, 검증 엔드포인트가 있는지 확인이 필요합니다.
  일부 공공 API는 국내 IP에서만 열려서 Next.js를 해외 리전에 올리면 서버사이드
  호출이 막힐 수 있습니다 - 배포 리전도 착수 전 확인 항목으로 적어 두었습니다.


[유사 프로젝트]

  광융합기업 산업지도 (LUMINA)
  https://jeonjaeman.github.io/demo-sites/lumina/
  줌 레벨 클러스터링·커스텀 마커·다중 필터·현재 영역 재검색·2분할↔바텀시트·
  가상 스크롤. 이번 건과 화면 구조가 가장 가깝습니다.

  플리마켓 셀러 매칭 앱 (BOOTHPIN)
  https://jeonjaeman.github.io/demo-sites/boothpin/
  지도 핀 탐색 → 하단 시트 상세. 모바일 지도 UX.

  AI 사이즈·핏 추천 커머스 (FORME)
  https://jeonjaeman.github.io/demo-sites/forme-fit/
  장바구니·결제·주문 내역까지 연결된 커머스 플로우.


[강점]

  - 웹디자이너 출신입니다. 지도 서비스는 한 화면에 지도·목록·필터·상세가 동시에
    들어가서 위계가 무너지면 아무것도 못 찾는 화면이 됩니다. 데모는 호갱노노의
    좌측 패널 376px와 필터 칩 규격(h32·r8·fs14)을 실측해 적용했습니다.
  - 기존 코드 인수가 이 과업의 실제 난이도라고 봅니다. 그래서 견적을 확정하기 전에
    코드를 먼저 보는 0차 검토를 무상으로 제안드립니다.
  - 수정 요청은 당일 반영합니다.


[견적 · 공수 기준]

  일 50,000원 기준으로 산정했습니다(부가세 별도).

  기존 코드 인수 · 화면정의서 대조 · 경계 합의        3일
  공통 퍼블리싱 (레이아웃·컴포넌트·반응형)            5일
  라우팅 · 전역 상태(Zustand) · 로딩/에러 UI          3일
  지도 (초기화·마커·클러스터링·오버레이·재조회)       6일
  매물 목록 · 필터 · 검색 (지도 연동)                 4일
  공공 API 바인딩 (프록시 경유 전제)                  3일
  OAuth · JWT (토큰 보관·갱신·인터셉터)               3일
  PG 결제 연동 · 결과 라우팅                          3일
  게시판 · 부가 기능                                  3일
  크로스 브라우징 · QA · 배포                         3일
  합계                                              36일 = 1,800,000원

  36공수는 계약 30일을 넘습니다. 그리고 기존 코드를 보기 전에는 이 숫자가
  확정되지 않습니다 - 인수 대상 코드의 상태가 공수를 크게 좌우하기 때문입니다.
  그래서 0차 검토에서 공수를 확정하고 1차/2차로 나누는 안을 함께 드립니다.


[의뢰자 부담 / 스코프 밖]

  서버·호스팅·도메인 · 공공데이터포털 API 키 및 트래픽 증가 신청 ·
  카카오 개발자 앱과 도메인 등록 · PG사 계약 및 상점 심사 ·
  백엔드 추가 개발(공공 API 프록시·결제 검증 엔드포인트가 없을 경우) ·
  이용약관 및 데이터 이용 범위에 대한 최종 판단.


데모를 직접 눌러 클러스터링을 꺼 보시고 결제 금액도 바꿔 보십시오.
바꾸고 싶은 부분을 말씀 주시면 반영해 보여드리겠습니다. 감사합니다.
