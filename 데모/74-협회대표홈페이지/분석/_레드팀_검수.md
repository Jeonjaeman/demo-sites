# 데모74 · 헤리가드공제조합 — 레드팀 종합 검수

검수자: 레드팀 4관점(디자인·인터랙션·QA실측·요구사항) · 대상 12페이지 · 뷰포트 1440/375
검수 도구: 브라우저 실측(getBoundingClientRect·getComputedStyle·textContent), 소스 대조, 실제 클릭 동작 확인
콘솔 에러: **전 12페이지 0건** (index·about·business·business-detail·verify·notice·notice-view·library·support·login·accessibility·admin)

> ⚙ 검수 환경 주의 (메인 세션 참고): 이 브라우저 팬은 `requestAnimationFrame`이 아예 동작하지 않고(1.5초에 0프레임) CSS 트랜지션도 정지 상태다. 그래서 스크롤 후 스크린샷이 백지로 나오고, 카운트업이 중간값(예: 6,342)에서 멈추며, `.rv` 리빌 요소가 opacity≈0으로 남는다. **이는 팬 한계이지 데모 버그가 아니다** — IntersectionObserver는 정상 발화해 `.in` 클래스는 붙었고(리빌 로직 정상), 카운트업은 rAF+easeOutCubic 표준 패턴이며 실제 브라우저에서는 완주한다. 아래 지적은 이 팬 아티팩트를 제외한 실제 결함만 담았다.

---

## ★ 심각도 '상' 요약 (반드시 교정)

1. **[상] FAQ 아코디언 전면 무동작 — business-detail.html · support.html**
   - 두 페이지의 인라인 스크립트 `render()`가 `.faq-q`에 클릭 핸들러(#1)를 붙이고, 동시에 `core.js`의 `initFaq()`가 DOMContentLoaded에서 같은 요소에 핸들러(#2)를 또 붙인다. 클릭 1회 → 두 핸들러가 각각 `classList.toggle('open')` 실행 → 열림+닫힘 상쇄 → **아무 일도 일어나지 않는다.** 1회·2회 클릭 모두 `open=false`, `aria-expanded=false` 실측 확인.
   - 정적 `.faq-item`은 저장소 어디에도 없음(둘 다 JS 주입) → **가장 간단한 수정: `assets/js/core.js` 151행의 `initFaq();` 호출 제거** (페이지별 render() 핸들러가 단일 소유자가 됨). 또는 반대로 두 페이지의 `forEach(...addEventListener('click'...))` 블록을 지우고 core.js initFaq만 남겨도 됨(initFaq는 render() 이후 실행되므로 주입된 항목을 잡음).

2. **[상] 관리자 상단바 375px 붕괴 — admin.html**
   - 공용 `.header .header-in`을 admin도 그대로 쓰는데, 공개 헤더는 모바일에서 `.gnb`·`.header-cta`를 숨기고 `.menu-btn`을 노출하지만 **admin의 우측 컨트롤(권한 전환 seg + 「사이트 보기」)에는 모바일 처리가 없다.** 375px에서 권한 버튼 「콘텐츠 담당」·「최고관리자」가 w44px로 짓눌려 **글자가 세로로 한 자씩 줄바꿈**되고, 「사이트 보기」는 right=381px로 **뷰포트(375) 밖으로 잘림**(실측).
   - 수정: 모바일(≤600px)에서 `body.admin-body .header-in { flex-wrap: wrap; height: auto; padding: 12px 20px; gap: 10px; }` + `body.admin-body .header-in .seg, body.admin-body .header-in .btn-ghost { flex-shrink: 0; }`. 필요 시 로고 텍스트(`.logo-txt`)를 모바일에서 숨겨 공간 확보.

3. **[상] 명도대비 4.5:1 미달 — 자체 접근성 선언과 모순 (전 공개 페이지)**
   - `--ink-3 #8a8a86`(흰 배경 위 **3.47:1**)이 날짜(`.n-date`), 카드 라벨(Guarantee/Mutual Aid/Credit Rating `.card-label`), 로고 서브라벨(HERIGUARD/ADMIN CONSOLE), 힌트(`.hint`), 로그인 진단 노트(`.d-note`)에 광범위하게 사용. 푸터 `.footer-bar` 색 `#5f6877`(#10151f 위 **3.25:1**)도 미달.
   - accessibility.html이 **KWCAG 2.2 준수·명도대비 4.5:1을 명시**하고 있어 선언과 실제가 어긋난다 → 심각도 상향.
   - 수정: `--ink-3`를 4.5:1 이상으로 어둡게(대략 `#6a6a66` 이하; 정확값은 대비 계산으로 확정). 푸터 `.footer-bar { color: #868f9e }` 수준으로 상향. (본문 `--ink-2 #4a4a48`는 통과하므로 유지.)

---

## ① 디자인 검수 (애플·삼성 시니어 웹디자이너 관점)

전반 평가: 공개 사이트는 기관형 톤을 잘 지켰다 — 네이비 #002f73 단일 브랜드, fade-up 단일 리빌, 대형 디스플레이(64px/-.04em), 140px 섹션 리듬, 에디토리얼 카피(플레이스홀더 슬롭 없음: "혁신적 솔루션" 류 전무). 히어로·about 인사말(단청 이미지)·verify·login 상단은 "사람이 의도해 만든" 완성도. 아래는 AI스러움/템플릿 냄새 지적.

- **[중] 관리자 내비 이모지 아이콘** (admin.html 30-40행) — `📊 📝 🖼 ♿ 📋 🔍 🔐 🔗` 컬러 이모지. 기관 관리자 콘솔에 컬러 이모지는 **장난감·AI 생성 인상**을 준다(애플/삼성이면 모노크롬 라인 SVG). notice-view의 첨부 `📎`도 동일. 수정: 이모지 → 인라인 라인 SVG 아이콘(1.5px stroke, 18px) 또는 텍스트 라벨.
- **[중] 반복 3열 카드** (index) — 「업무 3종」 grid-3 바로 아래 「이용 대상 3분기」 grid-3가 연속. 두 섹션 모두 3열·동일 리듬이라 템플릿 반복 인상이 약하게 있음. persona 카드에 `border-top:3px brand` 차별자가 있어 치명적이진 않으나, 리듬 변화(예: 이용대상 섹션을 2열 비대칭 또는 가로 스트립)를 주면 개선.
- **[하] 히어로 팝업 즉시 노출** (index) — 첫 로드에서 팝업(인터넷 영업점 점검)이 히어로 우측 퀵카드를 가림. 관리자 발행 팝업 데모라 의도적이나, 히어로의 핵심 CTA(발급조회 퀵카드)를 첫 화면에서 가리는 건 아쉬움. 등장 지연(예: 1.5초) 또는 위치 이동 고려.
- **정렬/간격 실측 양호** — grid-3 카드 top 3개 모두 1161px 동일 정렬, grid 컬럼 402.66/402.67/402.66px 균등, 가로 오버플로 전 페이지 0. 헤더 `aria-current` 밑줄 정확 표시. AI스러움 체크리스트 대부분 통과(과한 그림자·글로우·랜덤 액센트 없음, 위계 명확).

## ② 인터랙션 검수 (마이크로 인터랙션 큐레이터 관점 · 기관형이라 절제 전제)

핵심 인터랙션은 대부분 실제 동작한다. "죽어 보이는 정적 요소"는 아래 FAQ가 유일한 치명타.

- **[상] FAQ 클릭 무반응** — ①-1 참조. 사용자가 FAQ 질문을 눌러도 펼쳐지지 않아 "고장난 화면"으로 보임. 인터랙션 관점 최우선 결함.
- **[동작 확인됨] verify** — 예시 칩(유효/만료/금액불일치) → 조회 결과 렌더(마스킹 대○○○○○·금액 일치 판정), 조회 확인서 생성(검증번호 V-2026-000001·직인·면책), 연계 모드 토글이 실측 메타를 실제로 바꿈(신규 API 0.3초 ↔ 기존 연계 1.8초), 열거 공격 시연 로그. 전부 실동작.
- **[동작 확인됨] login** — 용도제한 인증서(전자세금용) 선택 시 실제 거부 메시지, 정상 인증서 → 브리지 3스텝 완주(인증서 서명→1회용 토큰 발급 POST /sso/token→영업점 세션, 성공 토스트). 타이머 기반 진행이 실제 진척.
- **[동작 확인됨] admin** — 오픈준비 체크리스트 체크 시 진행률 실시간 갱신(10%→20%, 바 width 갱신), 8개 패널 내비 전환, 접근성 검사가 실제 HTML 파싱해 alt 누락 탐지·발행 차단.
- **[중] 카운트업/리빌 무JS 취약** — 통계 카운트업·`.rv` 리빌은 정상 로직이나(팬 한계로 이 세션 검증만 불가), 콘텐츠가 JS 실행 전 `opacity:0`이라 **core.js 미로드 시 상당 콘텐츠가 영구 비가시**. 전산 인력 없는 발주처 특성상 신뢰성 위해 `<noscript>{.rv{opacity:1}}`류 폴백 권장. (심각도 중-하)
- **[하] 문의 폼 검증이 토스트만** (support) — 필수값 누락 시 `toast('이름과 내용을...')`로 차단은 되나 인라인 `.err-msg`·`aria-invalid` 없음(CSS의 `.field.err` 미사용). 접근성 폴리시로 인라인 오류 병행 권장.

## ③ QA 실측 검수 (브라우저 실측)

- 콘솔 에러: **전 페이지 0건** (`read_console_messages onlyErrors:true`).
- 가로 오버플로: 1440·375 전 페이지 **0** (문서 스크롤폭 = 뷰포트, 375에서 verify/admin 단일 컬럼 정상 붕괴, 표는 overflow 컨테이너 내부 스크롤 안전).
- 딥링크: `business-detail.html?type=guarantee/mutual/credit` → h1·타이틀·요율표 타입별 정확 분기. `notice-view.html?id=12` → 해당 공지 로드, 이전글/다음글/목록으로/첨부 존재.
- 이미지: 전부 로드됨(hero-palace 1920×1080, hanok-courtyard 1600×900, dancheong-hands 1200×900 등 고해상), 깨진 이미지 0.
- **[상] 관리자 상단바 375px 붕괴** — ①-2 참조(권한 버튼 w44 세로 줄바꿈, 「사이트 보기」 right=381>375 잘림). 실측 확인.
  - 참고: admin **가로 내비 행은 정상**(버튼 위치 순차 14→974, 3px 간격, 가로 스크롤). 초기 육안상 겹침은 오독이었음.
- **[중] 모바일 터치 타겟 44px 미만** (375, index) — persona 카드 CTA `.card-more`(「발급 사실 조회 →」·「조합원 로그인 →」·「가입 안내 →」) 높이 **24px**, 히어로 퀵링크 42px, 푸터 링크 31px, 로고 38px. persona 카드는 카드 자체가 링크가 아니라 `.card-more`만 탭 대상이라 24px는 실사용 불편. 수정: 모바일에서 `.card-more`에 세로 패딩(예: `padding:10px 0`)으로 탭 영역 확대. (모바일 햄버거 버튼은 44×44 정확, 드로어 포커스 이동 정상 — 양호.)
- **[하] 명도대비** — ①-3 참조(ink-3 3.47:1, 푸터 3.25:1).
- **[하] library 다운로드 MIME** — 실제 Blob 생성 확인(139B)되나 `text/plain`. 서식이 .pdf/.hwp 이름이면 콘텐츠타입 불일치. 데모로는 허용, 실구축 시 실제 문서로 교체.

## ④ 요구사항 충족 검수 (의뢰자 본인 관점 · 공고 대조)

공고 필수 과업을 체크리스트로 전개해 대조. **핵심 요구는 모두 충족**, 미흡은 아래 소수.

- [✓] **대외용 6메뉴** — 조합소개(about) · 조합업무(business) · 자료실(library) · 고객센터(support) · 공지사항(notice) · 발급사실조회(verify) 전부 독립 페이지로 존재. 회원가입·로그인 없이 접근 가능.
- [✓] **발급 사실 조회 비회원 무인증** — verify에서 로그인 없이 다중 대조 조회, 마스킹, 조회 확인서 출력까지 실동작.
- [✓] **관리자 직접 등록·수정** — admin에 공지·자료실·**블록 편집(메인 히어로 문구·업무 설명·연락처·조직도, 미리보기→발행→되돌리기)**·배너·팝업(게시 기간 필수, 종료일 없으면 저장 차단) 존재. 전산 인력 없이 운영 가능하게 설계됨.
- [✓] **공동인증서 로그인→영업점 연계 데모** — login.html 브리지 3스텝(인증서 서명→1회용 토큰 60초→영업점 세션)으로 재현. 용도제한 인증서 실거부 포함.
- [✓] **미확정 인증연계 3안 의견 화면 반영** — admin 「연계 진단」 패널이 3안(신규 API/기존 화면 연계/야간 미러 DB)을 조합 현재 조건으로 채점. verify 상단 연계 모드 토글로도 3안의 응답속도·점검 시 거동·개발부담 트레이드오프를 실측 비교로 노출. 공고가 명시 요청한 항목을 화면으로 답함.
- [✓] **신뢰감 디자인·모션 지양 준수** — 패럴랙스·핀·마퀴 없음, fade-up 단일 패턴+카운트업만. 공개 사이트 톤 적합. (단, admin 이모지 아이콘이 "전문성" 인상을 소폭 깎음 — ①-중 참조.)
- [△] **[하] 요율 "계산 예시"** — business-detail의 요율은 정적 표+예시 서술이고 **인터랙티브 계산기는 없음**(input[type=number]·calc 요소 미검출). 페이지 기획의 "요율(계산 예시)"가 정적 예시 의도면 충족, 상호작용 계산 의도였다면 미흡. 확인 후 필요 시 간단 계산 위젯 추가.
- [참고] 팝업 노출 범위 불일치 — 관리자 팝업은 data.js 로드 페이지(index·notice·library·support·business-detail)에서만 뜨고 about·business·verify·login·accessibility에는 안 뜸(해당 페이지가 data.js 미포함). 홈 위주 노출은 관행상 무방하나, 사이트 전역 팝업을 의도했다면 data.js 포함 또는 팝업 로직을 core 전용 데이터로 분리 필요.

---

## 잘된 점 (신뢰 형성용 · 유지 권장)

- 표면별 혼합 처리 정확: 공개 사이트=기관형(절제), admin=대시보드형(KPI·체크리스트·표·패널·권한 전환).
- 발견을 기능으로 구현: 인증연계 3안 → 실측 비교 토글/채점, 열거 공격 → 시연 로그, 용도제한 인증서 → 실거부, 접근성 → 발행 차단 검사. 문서 지적이 아니라 동작으로 답함.
- 입력 없이 클릭만으로 완주 가능(예시 칩·샘플 인증서·샘플 체크리스트). 실계산(마스킹·금액 대조·진행률·alt 파싱) 하드코딩 아님.
- 접근성 기본기: skip-nav, aria-current, 팝업 role=dialog+포커스 트랩+ESC, 드로어 포커스 이동, prefers-reduced-motion.
