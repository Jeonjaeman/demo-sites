{
  "name": "forme-fit-widget-prototype",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "python -m http.server 4173 --bind 127.0.0.1",
    "test": "playwright test tests/prototype.spec.js --reporter=line --workers=1"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
