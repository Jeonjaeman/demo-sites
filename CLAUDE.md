# 제안용 데모사이트 제작 지침

이 저장소는 **위시캣 등 외주 공고에 지원하기 위한 제안용 데모사이트** 모음입니다.
사용자가 **폴더명만 지정하면** 아래 워크플로를 처음부터 끝까지 자동으로 수행합니다.

> 예: "쇼핑몰연동핏 폴더 확인해서 진행해줘" → 0단계부터 8단계까지 전부 수행 후 결과 보고

---

## 핵심 원칙

**이 데모의 목적은 "만들 수 있다"가 아니라 "당신이 놓친 걸 봤다"를 증명하는 것입니다.**

경쟁 지원자가 50~80명입니다. 화면을 예쁘게 만드는 것만으로는 구별되지 않습니다.
**요구사항대로 만들면 실패하는 지점을 찾아내고, 그 대안을 화면으로 이미 구현해 놓는 것**이 이 작업의 본질입니다.

따라서 매 건마다 반드시:
1. **시나리오로 니즈를 재구성**한다 (기능 목록 → 사용 흐름)
2. **웹 리서치로 치명적 결함을 찾는다** (법적·기술적·운영적)
3. **찾은 문제를 설명이 아니라 기능으로 구현한다**

---

## ★ 최우선 원칙 — 레퍼런스가 지정되면 "참고"가 아니라 "재현하고 능가한다"

**사용자나 공고가 레퍼런스 사이트를 지정하면, 그 사이트를 “참고”하는 게 아니라
그 사이트를 만든 최고의 웹디자이너가 최고 퍼포먼스로 다시 만든다는 각오로 재현하고 능가한다.**
"영감을 받았다" 수준의 결과물은 **30점**이고, 레퍼런스와 나란히 놓아도 밀리지 않는 결과물이 **100점**이다.
이 저장소의 데모는 항상 100점을 목표로 한다.

### 레퍼런스를 섹션 단위로 전수 해부한다 (실측을 이 수준까지)
브라우저로 레퍼런스를 열고 **처음부터 끝까지 스크롤하며 모든 섹션을 해부**한다. 색·폰트 몇 개
뽑는 걸로 끝내지 않는다. 아래를 **섹션마다** 기록한다:

| 축 | 반드시 뜯어올 것 |
|---|---|
| **화면 구성** | 섹션 순서·개수, 각 섹션의 레이아웃 골격(풀블리드/컨테이너, 그리드 컬럼, 좌우 비대칭, 오버랩), 히어로 구조, 여백 리듬(섹션 상하 패딩 px) |
| **타이포** | 디스플레이 실측 크기(px)·weight·line-height·letter-spacing, 국문/영문 폰트 분리, 본문 대비 배율 — **크기와 과감함을 그대로 가져온다.** 레퍼런스가 64~96px 대형이면 우리도 그만큼 크게 |
| **이미지·영상 퀄리티** | 히어로가 영상인가/풀블리드인가, 이미지 비율·처리(오버레이·마스크·그레인), 몇 장을 쓰는가. **레퍼런스가 시네마틱·에디토리얼이면 우리 생성물도 같은 프로덕션 퀄리티로.** 저퀄 저해상·어정쩡한 스톡 느낌 금지 |
| **단락 카피** | 문장의 **목소리**(에디토리얼·감성·브랜드 보이스)와 길이·리듬. 기능 나열식 불릿이 아니라 레퍼런스처럼 쓴다. 예: 삼화의 “산뜻한 아쿠아틱 제이드와 은은한 그레이 미스트가 조화를 이루며 청량하고 여유로운 공간감을 만들어냅니다” 같은 문장형 카피 |
| **인터랙션 카피** | 버튼·탭·호버·상태마다의 문구(마이크로카피)를 레퍼런스 톤으로. 빈 상태·로딩·성공 문구까지 |
| **스크롤 애니메이션** | 각 섹션이 **어떻게 등장하는가** — 패럴랙스, 핀 고정, 텍스트 마스크/라인 리빌, stagger 순서·간격, 이미지 스케일 인, 카운트업, 가로 스크롤, 이징 커브·지속시간. 레퍼런스의 “움직이는 느낌”을 그대로 재현 |

`getComputedStyle`로 수치를 뽑고(`lab()`/`oklch()`는 3단계 B의 canvas 기법), `get_page_text`로 카피
목소리를 읽고, 스크롤 위치를 바꿔가며 애니메이션 트리거를 관찰한다.

### 만들 때
- **타이포를 작게 타협하지 않는다.** 레퍼런스가 과감하면 우리도 과감하게(대형 디스플레이·강한 자간).
- **이미지·영상은 레퍼런스와 같은 급으로 생성한다.** 필요하면 여러 번 재생성해 최고 컷을 고르고,
  풀블리드·시네마틱·에디토리얼로 배치. 장식 이미지 한두 장으로 때우지 않는다.
- **스크롤 연출을 촘촘히 넣는다.** 리빌 하나로 끝내지 말고 섹션마다 성격에 맞는 연출(패럴랙스·핀·마스크
  리빌·가로 스크롤·이미지 스케일)을 설계한다. 단, 4단계 모션 강도표와 성능(첫 렌더·`prefers-reduced-motion`)은 지킨다.
- **카피를 브랜드 보이스로 새로 쓴다.** 레퍼런스의 문장 톤을 흡수해 감성·에디토리얼로.
- **여백·정렬·정보 위계**를 레퍼런스만큼 정제한다. 카드 그리드 반복으로 때우지 않는다.

### 자기 검열 (완료 전 필수)
완료 보고 전에 **레퍼런스와 내 결과물을 나란히 놓고 스스로 점수를 매긴다.** “레퍼런스를 아는 사람이
내 데모를 보면 같은 급으로 볼까, 아니면 싸구려 카피로 볼까?” 30~70점이면 **다시 만든다.**
화면 구성·타이포 스케일·이미지/영상 퀄리티·카피 목소리·스크롤 연출 중 하나라도 레퍼런스에 확연히
밀리면 완료가 아니다.

> 이 원칙은 3단계 B(디자인 레퍼런스)·4단계(구축)·5단계(검증)를 관통한다. 레퍼런스가 지정된 건은
> "매칭되는 게 없다"가 성립하지 않을 뿐 아니라, "적당히 참고했다"도 성립하지 않는다. **재현하거나 능가한다.**

---

## ★ 레드팀 디자인 검수 — "애플 웹디자이너" 관점으로 AI스러움을 잡아낸다

**구축(4단계) 후, 배포(7단계) 전, 반드시 레드팀 디자인 검수를 거친다.** 레드팀은 **애플 공식
웹사이트를 만드는 시니어 웹디자이너** 페르소나다. 자신이 만든 화면을 스스로 칭찬하지 말고,
이 페르소나로 **입장을 바꿔 적대적으로 전체 디자인을 검수**한다. 역할은 두 가지다 —
**① "AI스러움"을 콕 집어 지적하고 ② 애플 수준의 감각으로 직접 교정한다.**

> 레드팀은 "빌더가 만든 것 같다 / 템플릿 같다 / AI가 뽑은 것 같다"는 인상을 **결함으로 취급한다.**
> 예쁘장한데 어디서 본 것 같으면 그건 실패다. 애플 사이트는 "이건 사람이, 그것도 최고가
> 의도해서 만들었다"가 느껴진다. 그 격차를 없애는 게 레드팀의 일이다.

### AI스러움 체크리스트 (하나라도 걸리면 교정)
- **뻔한 반복 구조**: 아이콘+제목+한 문단 카드가 섹션마다 3열로 반복. 모든 섹션이 같은 리듬.
- **의미 없는 장식**: 목적 없는 그라디언트·글로우·글래스모피즘·블러, 보라→파랑 "AI 스타트업" 히어로.
- **과한 둥근 모서리·그림자 도배**, 모든 카드에 동일한 드롭섀도.
- **이모지·랜덤 액센트 색 남발**, 브랜드와 무관한 색이 여기저기.
- **정렬·간격 불일치**: 그리드가 눈에 안 맞음, 옵티컬 정렬 무시, 좌우 여백 제각각.
- **위계 없음**: 제목·본문·캡션이 다 비슷한 크기·굵기. 무엇을 먼저 볼지 안 보임.
- **중앙정렬 단조로움**: 모든 걸 가운데 정렬해 리듬이 죽음.
- **목적 없는 모션 과다**: 전부 페이드인, 아무 데나 통통 튀는 애니메이션.
- **플레이스홀더 카피**: "혁신적인 솔루션" 류의 알맹이 없는 문구, 채워넣은 티.
- **성의 없는 이미지**: 저해상·스톡 느낌·주제와 안 맞는 사진, 크롭·비율·톤 제각각.

### 애플 감각으로 교정 (레드팀의 기준)
- **극단적 절제.** 요소를 더하지 말고 뺀다. 장식이 아니라 **여백·타이포·정렬**로 말한다.
- **명확한 위계.** 한 화면에 주인공은 하나. 크기·굵기·색·여백으로 시선 순서를 설계한다.
- **정밀함.** 8pt 그리드·일관된 간격 스케일·옵티컬 정렬. 1px도 우연이 없다.
- **콘텐츠 우선.** 실물(제품·이미지·숫자)이 주인공이고 UI는 물러선다. 큰 이미지, 큰 여백.
- **목적 있는 모션만.** 전환·초점 유도에만 절제해서. 화려함이 아니라 **부드러움과 정확함**.
- **소재감으로 프리미엄.** 값싼 그라디언트 대신 대비·질감·정밀한 타이포·고품질 이미지.
- **일관성.** 색·radius·간격·버튼·모션이 사이트 전체에서 한 시스템으로 움직인다.

### 절차
1. 완성된 화면을 **섹션마다** 레드팀 눈으로 훑으며 AI스러움을 **목록으로 적는다**(막연히 "괜찮다" 금지).
2. 각 지적을 **실제로 교정**한다(코드 수정). 지적만 하고 넘어가지 않는다.
3. 교정 후 다시 훑어 "애플이 만들었다고 해도 믿을 수준인가"를 판정. 아니면 반복.
4. 보고에 **레드팀이 잡은 AI스러움과 교정 내용**을 함께 적는다.

> 4단계 자기 검열(레퍼런스와 나란히)과 이 레드팀 검수(AI스러움 제거)를 **둘 다** 통과해야 100점이다.
> 레퍼런스가 지정되지 않은 건도 레드팀 검수는 **항상** 한다 — AI스러움은 어느 프로젝트에나 낀다.

---

## ★ 필수 섹션 구성 인벤토리 — 매 건, 애플·삼성 수준으로 구현

**이제부터 데모를 만들 때마다 아래 섹션 구성을 콘텐츠에 맞게 반드시 포함한다.**
벤치마크는 **애플(apple.com 제품 페이지)과 삼성(samsung.com)** 이며, 같은 수준의 퍼포먼스
(부드러움·정밀함·고퀄 이미지·스크롤 연출)를 구현한다. "카드 3열 반복"으로 때우지 않는다.

이 섹션들의 공통 전제: **고퀄 이미지가 핵심 재료다.** 저해상·스톡 느낌 금지. Higgsfield로
2k 생성 → 다운로드 → Pillow/ffmpeg 최적화해서 쓴다(4단계 이미지 규칙). 이미지 없이 색면으로
때우지 않는다.

### ① 마우스에 반응하는 인터랙션 카드 섹션 (이미지 배경 카드)
- 카드마다 **고퀄 이미지 배경**, 마우스에 반응한다 — 3D 틸트(`rotateX/rotateY`)·포인터 추적
  하이라이트(스포트라이트)·이미지 시차/줌 중 하나 이상. `pointermove` → `transform`만 갱신.
- 성능: rAF 스로틀·`will-change` 관리, 데스크톱만 틸트, **모바일은 틸트 끄고 탭/스크롤로 대체.**

### ② 풀블리드 이미지 배경 단락 (타이틀 · 작은 타이틀 · 본문)
- **가로를 꽉 채운(full-bleed)** 섹션, 고퀄 이미지 풀블리드 배경.
- 그 위에 **작은 타이틀(eyebrow) · 대형 타이틀 · 본문** 위계. 가독성용 그라디언트 오버레이.
- 스크롤 시 배경 **패럴랙스/천천히 줌**. 애플 제품 페이지의 대형 히어로 카피 섹션 톤.

### ③ 풀블리드 캐러셀 (이미지 배경 카드들의 모음)
- **가로를 꽉 채운 캐러셀**, 카드마다 **고퀄 이미지 배경**.
- 드래그/스와이프·화살표·자동전환·스냅. 삼성처럼 카드가 옆으로 흐르거나 애플처럼 스냅 이동.
- 카드 위 최소 카피(카테고리+타이틀), 호버 시 확대/오버레이. 키보드 접근·`aria` 지원.

### ④ 풀블리드 다분할 그리드 (셀마다 이미지)
- **가로를 꽉 채운 4분할 또는 다분할 그리드**, **각 셀이 고퀄 이미지**.
- 비대칭(벤토 그리드) 허용. 셀 호버 시 줌/오버레이/카피 등장. 모바일에서 1~2열로 재배치.

> ★★ "가로 꽉 채움 = 100vw"의 정확한 의미 (②③④ 공통, 오해 금지)
> **"너비를 꽉 채운다"는 컨테이너(예: max-width 1200~1320px shell) 안을 채우라는 뜻이 아니라,
> 화면 좌우 끝까지 = 뷰포트 전체 폭(100vw)을 채우라는 뜻이다.** 대부분의 템플릿은 본문을
> 중앙 컨테이너에 가둔다. 이 섹션들은 그 컨테이너를 **벗어나** 화면 끝에서 끝까지 이미지를 깐다
> (애플·삼성 제품 페이지의 풀블리드 갤러리처럼).
>
> - 구현: 그리드/미디어 래퍼에 `width:100vw; margin-left:calc(50% - 50vw);` (컨테이너를 탈출해 100vw).
> - **제목·본문 같은 읽는 텍스트는 컨테이너(shell)에 두고, 이미지 그리드/미디어만 100vw로** 뺀다.
>   (텍스트까지 화면 끝에 붙이면 가독성이 나쁘다.)
> - 셀이 화면 끝에 딱 붙어 라운드가 잘리는 게 싫으면 **아주 작은 인셋(12~24px)** 만 준다.
>   단, shell 게터(수십~88px)를 그대로 쓰면 "컨테이너 폭"이 되어 100vw가 아니다 — 착각 금지.
> - `html { overflow-x: clip }`(또는 hidden)으로 100vw가 만드는 가로 스크롤바를 없앤다.
> - ②(풀블리드 이미지 단락)·③(풀블리드 캐러셀)도 동일하게 **섹션/미디어가 100vw**여야 한다.

### ⑤ 텍스트 애니메이션
- 스크롤 연동 텍스트 리빌 — **라인 마스크·단어/글자 stagger·스크롤 스크럽**(스크롤에 따라
  텍스트가 채워지거나 나타남) 중 하나 이상을 핵심 카피에 적용.
- 애플의 "스크롤에 따라 문장이 또렷해지는" 연출 톤. 과하지 않게, **정확하고 부드럽게.**

### 적용·가드
- 사이트 성격(대시보드·폼·챗 등)상 전부가 안 맞으면 성격에 맞게 변형하되, **최소한 ②(풀블리드
  이미지 단락)와 ⑤(텍스트 애니메이션)는 항상 넣는다.** 나머지도 가능한 한 포함한다.
- 4단계 **모션 강도표**·성능 규칙(첫 렌더 지연 금지, `prefers-reduced-motion` 존중, `transform`
  기반)과 **레드팀 검수**를 그대로 지킨다. 화려함이 목적이 아니라 **애플·삼성급 완성도**가 목적이다.
- 5단계 검증에 이 섹션들의 동작(마우스 반응·캐러셀·그리드 호버·텍스트 애니메이션)을 포함한다.

---

## 워크플로

### 0단계 — 요구사항 확보

지정된 폴더를 열어 원본을 확인합니다.

| 원본 형태 | 처리 |
|---|---|
| `.txt` 공고문 있음 | 그대로 읽고 진행 |
| 이미지(webp/png)만 있음 | `Read`로 전사 → `원본기획/`으로 이미지 이동 → `기능명세(의뢰자 원본).txt` 생성 |
| 예산·기간 없음 | 기능 범위로 역산 (아래 견적 규칙) |

- 원본 파일은 **절대 삭제·수정하지 않습니다.** 전사본을 따로 만듭니다.
- 폴더가 비어 있거나 원본을 못 찾으면 **거기서 멈추고 사용자에게 확인**합니다.

### 1단계 — 시나리오 기반 니즈 분석

기능 목록을 **시간순 사용 흐름**으로 재배치합니다. 이것이 이 저장소의 표준 분석 방식입니다.

1. **등장인물 정의** — 누가 쓰나 / 무엇을 원하나 / 무엇을 불안해하나
   - 한 사람이 두 역할을 겸하는지 반드시 확인 (예: 주최자↔셀러, 개최↔참여)
2. **시간순 여정** — 발견 → 판단 → 행동 → 대기 → 실행 → 사후
   - 각 단계마다: **상황 / 쓰는 화면 / 이 기능이 없으면 벌어지는 일**
3. **역할별 시야 차이** — 같은 데이터를 역할에 따라 다르게 보는 지점 = **권한 설계의 핵심**
4. **명시된 니즈 vs 숨은 니즈** 분리
   - 숨은 니즈 단서: "기획 상태", "프로젝트 경험 없음", "월 유지보수 예정", "○○로 하는데 번거롭다"
5. **비는 구멍 도출** — 시나리오를 그리면 정의가 없는 예외 상황이 드러납니다
   - 취소되면? 이탈하면? 분쟁나면? 마감은? 정산 시점은? 신고는? 운영자 화면은?
   - 이것이 `시나리오_사용자여정.md` 7장이 됩니다

### 2단계 — 웹 리서치 (필수, 생략 불가)

**최소 4회 검색.** 근거 URL을 반드시 확보합니다.

검색해야 할 축:
- **법적 리스크** — 신고·등록 의무, 형사처벌, 과태료, 개인정보, 저작권
- **기술적 함정** — 공고에 명시된 스택이 실패하는 지점
- **도메인 실무** — 그 업계에서 실제로 문제가 되는 것
- **표준 관행** — 이 분야에서 통용되는 설계 방식

**우선순위:** `그대로 만들면 작동하지 않음` > `법 위반 소지` > `운영 중 반드시 터짐` > `있으면 좋음`

지금까지 나온 치명적 발견 예시(참고):
- 국세청 API는 신규 사업자 *목록*을 제공하지 않음 → 1차 수집 자체가 불성립
- Streamlit + 단일 CSV = 동시 접속 시 데이터 유실
- 교장·교감은 학교당 1명 → 지역별 집계 시 개인 특정
- 등급 규칙에 절대·상대 기준 혼용 → 서열 역전, 강등 발생
- 지도 기능 = 위치기반서비스사업 신고 대상 (미신고 시 형사처벌)

### 3단계 — 레퍼런스 실측 (★ 두 축을 분리한다)

레퍼런스는 **목적이 다른 두 종류**입니다. 섞으면 안 됩니다.

| | **A. 도메인 레퍼런스** | **B. 디자인 레퍼런스** |
|---|---|---|
| 목적 | **무엇을 만들까** — 니즈 분석·기획 | **어떻게 보이게 할까** — 퀄리티 |
| 선정 기준 | 같은 업종·같은 기능의 서비스 | **작업 내용과 무관.** 디자인 어워드 수상작 |
| 개수 | **3곳** | **2~3곳** |
| 가져오는 것 | IA·화면 구성·콘텐츠 문법·업계 관행 | 타이포 스케일·여백 리듬·모션 언어·레이아웃 감각 |
| 문서 표기 | 분석 문서 3장 표 ① | 분석 문서 3장 표 ② |

**공통 원칙:** 추측하지 말고 브라우저로 직접 열어 실측합니다.
`preview_start {url}` → `javascript_tool`로 `getComputedStyle` 추출.
WebFetch가 403/500이면 **반드시 브라우저로 전환**합니다(SPA는 fetch로 안 잡힘).
네이버 블로그 등 **정책상 차단되는 도메인**은 브라우저로도 안 열립니다. 이때는 링크 내용을
추측하지 말고 사용자에게 본문을 요청하거나, **후보 사이트를 직접 접속 테스트해 대체**합니다.

**`lab()`·`oklch()` 색상이 나올 때 (Tailwind v4 등 최신 사이트)** ★
`getComputedStyle`이 `lab(2.73 0.25 -0.93)`을 반환하고 canvas `fillStyle`로도 변환되지 않습니다.
1×1 canvas에 칠한 뒤 `getImageData`로 픽셀을 읽어야 hex가 나옵니다.

```js
const cv = document.createElement('canvas'); cv.width = cv.height = 1;
const c = cv.getContext('2d', { willReadFrequently: true });
const hex = v => { c.fillStyle = '#fff'; c.fillRect(0,0,1,1); c.fillStyle = v; c.fillRect(0,0,1,1);
  const d = c.getImageData(0,0,1,1).data;
  return '#' + [d[0],d[1],d[2]].map(x => x.toString(16).padStart(2,'0')).join(''); };
```

이 기법 없이는 Airbridge·Vercel 계열 최신 사이트의 색 실측이 통째로 막힙니다.

---

#### A. 도메인 레퍼런스 3곳 — 기획을 위한 실측

같은 업종·기능의 서비스를 찾습니다. 조합 권장: **국내 1 + 해외 2**(국내 감성 + 글로벌 완성도).
공고에 참조 대상이 명시되어 있으면(예: "ChatGPT 디자인 참조") **그것을 1순위**로 넣습니다.

**추출 항목 (색만 보지 말 것)**

| 축 | 추출할 것 |
|---|---|
| 색 | 배경·잉크·보조·경계선·액센트, CSS 변수(`--*`) 통째로 |
| 타이포 | 폰트 패밀리, h1 크기/weight/**letter-spacing**/line-height, 본문 대비 |
| 형태 | border-radius(버튼·카드 각각), 그림자, 경계선 두께·색 |
| 리듬 | 컨테이너 max-width, 섹션 상하 여백, 카드 간격, 그리드 컬럼 |
| 컴포넌트 | 버튼 높이·패딩·weight, 입력창 포커스 스타일, 뱃지·칩 |

**레이아웃·콘텐츠 구성도 함께 관찰합니다** — 색과 폰트만 베끼면 껍데기입니다.
정보 위계(무엇을 크게/먼저 보여주는가), 섹션 순서, 카드 안 정보 배치, 빈 상태 처리, 스크롤 시 무엇이 고정되는가.
**업계 공통 문법이 보이면 기록합니다** (예: 국내 교육 플랫폼은 레드 CTA + 상단 프로모션 바가 표준).

---

#### B. 디자인 레퍼런스 2~3곳 — 퀄리티를 위한 실측

**의뢰 내용과 전혀 상관없어도 됩니다.** 목적은 "이 정도 완성도"의 기준선을 가져오는 것입니다.
도메인 레퍼런스만 보면 그 업계 평균 수준에 머뭅니다. 경쟁 지원자와 갈리는 지점이 여기입니다.

> ### ★ 매칭 실패를 이유로 건너뛰지 않습니다
>
> **"성격이 비슷한 수상작이 없다", "이 업종은 레퍼런스가 없다"는 판정을 내리지 않습니다.**
> 레퍼런스는 업종으로만 찾는 게 아니라 **화면 유형·섹션·컴포넌트 단위로도 찾습니다.**
> 아래 폴백을 순서대로 내려가면 어떤 프로젝트든 반드시 걸립니다.
>
> | 순서 | 매칭 축 | 소스 |
> |---|---|---|
> | ① | 같은 성격의 **수상작** (랜딩↔랜딩, 대시보드↔데이터 제품) | Awwwards · CSSDA · FWA · GDWEB |
> | ② | **화면 유형·패턴** (로그인·온보딩·검색·장바구니·주문·마이페이지·빈 상태·404) | Refero · **WWiT**(국내 앱 18패턴) |
> | ③ | **업종·카테고리** (식품·금융·패션·의료·부동산·배달·모빌리티·SaaS) | DBCUT(국내) · WWiT · Lapa Ninja |
> | ④ | **섹션·컴포넌트** (히어로·가격표·푸터·입력창·탭·바텀시트·팝업·차트) | Land-book(섹션) · **WWiT**(컴포넌트) |
> | ⑤ | **그래픽·모션 톤** (무드·질감·전환 감각) | **Cosmos** |
> | ⑥ | 그래도 없으면 **최신 SOTD 아무거나** | 이식 가능한 값(이징·자간·여백)만 가져옵니다 |
>
> 업종이 안 맞아도 ②·④는 **어떤 프로젝트에도 반드시 존재합니다.** 로그인 없는 서비스,
> 빈 상태 없는 목록, 입력창 없는 폼은 없습니다. 여기서 막히는 경우는 없습니다.
>
> **어떤 경우에도 아래 4축은 비우지 않습니다.** 이것이 B의 최소 산출물입니다.
>
> | 축 | 반드시 뽑아 올 것 |
> |---|---|
> | **디자인 시스템** | 색 토큰(배경·잉크·보조·경계선·액센트) · 타이포 스케일 · radius · spacing 스케일 |
> | **UI/UX** | 레이아웃 골격 · 정보 위계 · 내비게이션 · 빈 상태·에러 처리 · 모바일 전환 방식 |
> | **인터랙션** | 이징 커브 · 지속시간 · stagger 간격 · 호버/포커스/액티브 상태 · 스크롤 연출 |
> | **그래픽 소스** | 아이콘 스타일(선 굵기·라운드) · 일러스트/패턴 사용법 · 이미지 처리(비율·오버레이·마스크) |

**소스 — 2026.07 브라우저 접속 확인**

*어워드 (퀄리티 기준선 · 수상작)*

| 사이트 | 용도 |
|---|---|
| [Awwwards](https://www.awwwards.com/) | SOTD / Honorable Mention — 종합 최고 수준. 카테고리 목록에서 수상 배지 확인 가능 |
| [CSS Design Awards](https://www.cssdesignawards.com/) | WOTD. **갤러리 목록은 대부분 후보** — 상세에서 수상 여부 확인 필수(아래 절차) |
| [FWA](https://thefwa.com/) | 인터랙션·모션 중심 |
| [GDWEB 지디웹](https://www.gdweb.co.kr/main/) | **국내 웹어워드.** 연도별 선정작 · WEB/MOBILE 부문 분리 — 국내 감성 기준선 |

*갤러리 (매칭용 · 수상작이 아니어도 됨)*

| 사이트 | 매칭 축 |
|---|---|
| [WWiT](https://wwit.design/) | ★ **국내 앱·서비스 UI 아카이브.** 3축 분류가 폴백과 정확히 맞습니다<br>업종 `/tag/` life·finance·commerce·realestate·delivery·entertainment·mobility<br>**패턴** `/pattern/` splash·signin·signup·onboarding·home·search·notification·select·loading·success·cart·order·list·review·password·t&c·mypage·setting (18종)<br>**컴포넌트** `/component/` input·tab·bottomsheet·popup·chart<br>수록: 알라미·리볼트·mmm·네이버페이·뤼튼·삼쩜삼·치지직·헤이딜러 등 |
| [Refero](https://refero.design/) | **화면 유형별** — Dashboard·Login·Paywall·Product Details·Catalog·404·Careers 등 |
| [DBCUT 디비컷](https://www.dbcut.com/) | **국내 업종별** 9,800여 건 — 식품·금융·패션·의료·기계·인터넷 등 전 업종 |
| [Lapa Ninja](https://www.lapa.ninja/) | **카테고리별 랜딩** 7,400여 건 — SaaS·이커머스·AI·앱·포트폴리오. 풀페이지 스크린샷 제공 |
| [Land-book](https://land-book.com/) | **섹션 단위** 탐색 — Sections·Mobile·Motion·OG Images·Headlines 뷰 |
| [One Page Love](https://onepagelove.com/) | 랜딩 페이지 특화 — 모객·전환형 프로젝트 |

*무드·그래픽 (★ 실측 대상이 아닙니다)*

| 사이트 | 용도 |
|---|---|
| [Cosmos](https://www.cosmos.so/explore/motion) | 이미지·영상 큐레이션. `/explore/` motion · ui-ux · graphic-design · typography · branding · art · technology 등<br>**라이브 사이트가 아니라 이미지·영상이라 `getComputedStyle` 실측이 불가능합니다.**<br>여기서 가져오는 것은 수치가 아니라 **그래픽 소스 방향과 모션 톤**입니다 —<br>아이콘·일러스트 스타일, 질감, 전환 감각. 4축 중 「그래픽 소스」를 채울 때 씁니다 |

> **실측(수치) 소스와 무드(방향) 소스를 섞어 적지 않습니다.**
> 분석 문서에 Cosmos를 적을 때는 `무드 참고 — 실측값 없음`을 함께 표기하고,
> 색·타이포·이징 **수치는 반드시 라이브 사이트에서 뽑습니다.**

*접근 주의*

| 사이트 | 상태 |
|---|---|
| [Godly](https://godly.website/) | **recent.design으로 리다이렉트**되고 목록이 JS 렌더라 추출되지 않습니다. 쓰지 마세요 |
| [SiteInspire](https://www.siteinspire.com/) | 429 잦음. 브라우저로 시도하되 실패하면 위 목록으로 대체 |
| 네이버 블로그·카페 | **정책상 차단.** 열리지 않습니다 |

**수상 여부 확인 절차 (★ 후보를 수상작으로 적지 않기)**

| 소스 | 확인 방법 |
|---|---|
| Awwwards | 카테고리·SOTD 목록의 배지 텍스트에 `SOTD Site Of The Day <날짜>`가 들어 있습니다. **목록만으로 판별 가능** |
| CSSDA | 갤러리 목록은 대부분 *Nominated*입니다. 상세 페이지를 열어 **심사 평균 8.0 이상**인지 확인해야 WOTD입니다 |
| GDWEB | 연도별 선정작 / FINALIST 페이지에서 확인 |

**수상 내역을 명시할 수 없으면 「어워드」로 세지 않고 「갤러리 레퍼런스」로 적습니다.**
`(Awwwards 다수 수상)` 처럼 근거 없이 뭉뚱그리지 않습니다. 최소 1곳은 어워드에서 채웁니다.

**선택 기준**
- 만들 사이트와 **성격이 비슷한 수상작**을 우선합니다 (랜딩↔랜딩, 대시보드↔데이터 제품)
- 업종은 달라도 됩니다. **완성도의 기준선**을 가져오는 것이 목적입니다
- 수상 연도가 최근일수록 좋습니다 (트렌드 반영)
- **국내향 서비스면 GDWEB·DBCUT·WWiT을 최소 1곳 섞습니다.** 해외 수상작만 보면 국내 이용자에게
  낯선 정보 밀도가 나옵니다
- **모바일 앱 성격이면 WWiT을 필수로 넣습니다.** 국내 앱의 온보딩·바텀시트·마이페이지 관행은
  해외 갤러리에서 안 나옵니다

**가져오는 것 (색보다 이것들)**

| 축 | 관찰할 것 |
|---|---|
| 타이포 스케일 | 수상작은 헤드라인이 과감합니다 — h1 실측값, 본문과의 대비 배율, letter-spacing 음수 폭 |
| 여백 리듬 | 섹션 상하 패딩이 큽니다(80~140px). 여백이 곧 급 차이를 만듭니다 |
| 모션 언어 | 이징 커브(`cubic-bezier`), 지속시간, stagger 간격, 스크롤 연출 방식 |
| 레이아웃 감각 | 비대칭, 오버랩, 그리드 브레이크, 큰 이미지 활용 |
| 마이크로 인터랙션 | 호버 상태, 커서 반응, 로딩·전환 처리 |
| 절제 | 색을 몇 개나 쓰는가. 대개 **2~3색 + 뉴트럴**입니다 |

**적용 규칙 — 그대로 베끼지 않습니다**
- 수상작의 파격을 **대시보드·백오피스에 그대로 넣으면 안 됩니다.** 4단계 모션 강도표를 따릅니다
- 가져올 것은 **타이포 대비·여백 리듬·이징 값** 같은 이식 가능한 요소입니다
- 실사용성을 해치는 연출(스크롤 하이재킹, 과한 로딩 인터랙션)은 버립니다
- **의뢰자 업종의 톤을 이깁니다** — 예: 소상공인 대상이면 수상작의 미니멀을 그대로 쓰지 말고 정보 밀도를 지킵니다
- **폰트는 못 가져옵니다.** 수상작은 대개 유료·커스텀 폰트를 씁니다
  (Dragonfly = FK Roman Standard / TRIONN = neueHaas·familjen / Triple Whale = Graphik).
  가져오는 것은 폰트가 아니라 **메트릭**입니다 — 자간을 **px이 아니라 em 비율로 환산**해
  (예: 185.5px에 −12.24px = **−0.066em**) Pretendard에 적용하고, line-height 배율·weight도 같은 방식으로 옮깁니다

**문서화**
- 분석 문서 3장에 **표 두 개**로 나눠 적습니다 — "① 도메인 레퍼런스", "② 디자인 레퍼런스"
- ②에는 **출처 종류(어워드/갤러리)·수상 내역·무엇을 가져왔는지**를 함께 적습니다
  (예: `Awwwards SOTD 2026.03 — 섹션 패딩 128px·h1 ls −2.4px·이징 cubic-bezier(.22,.61,.36,1)`)
  (예: `Refero · Dashboard 유형 — 좌측 네비 264px·빈 상태 일러스트 없이 텍스트 2줄`)
- **디자인 시스템 / UI·UX / 인터랙션 / 그래픽 소스 4축**을 각각 어디서 가져왔는지 표에 드러냅니다
- CSS 최상단 주석에도 두 출처를 모두 남깁니다

### 4단계 — 데모 구축

**발견한 문제를 설명이 아니라 "동작하는 기능"으로 만듭니다.**

| 발견한 문제 | 나쁜 대응 | 좋은 대응 (실제로 한 것) |
|---|---|---|
| 소규모 집단 개인 특정 | 문서에 "주의 필요" | 필터 좁히면 **실제로 비공개 처리**되는 화면 |
| 표본 대표성 부족 | 문서에 "가중 필요" | **가중 토글** — 누르면 수치가 진짜 바뀜 |
| 등급 규칙 충돌 | 문서에 "재설계 권장" | **등급 진단 화면** — 실제 분포로 계산해 충돌 노출 |
| 크롤링 차단 우회 위법 | 문서에 "위험함" | 파이프라인에서 **우회 모듈 제외 + 준법 수집 표기** |

**필수 구성**
- 목데이터는 **실제로 계산**되게 만듭니다 (하드코딩 금지). 필터·집계·등급은 진짜 연산.
- **리스크 검토 화면**을 반드시 앱/사이트 안에 넣습니다 (`검토 항목`, `등급 진단` 등)
- 다운로드는 **Blob으로 실제 파일 생성** (CSV는 UTF-8 BOM으로 엑셀 한글 대응)
- 외부 라이브러리 없이 canvas/CSS로 차트 구현 (dpr 대응 필수)
- 모바일 앱 성격이면 **폰 프레임 + 화면 인덱스** 구조 (데스크톱), 모바일에선 전체화면 전환

**인터랙션·모션 (정적 페이지 금지)**

"동적인 사이트"가 요구의 일부입니다. 색과 레이아웃만 베끼면 껍데기이므로 **움직임까지 설계**합니다.
다만 사이트 성격에 따라 강도를 다르게 합니다 — **과한 모션은 신뢰를 깎습니다.**

| 성격 | 모션 강도 | 넣을 것 |
|---|---|---|
| 브랜드 홈페이지·랜딩 | **강** | 히어로 패럴랙스, 스크롤 리빌(stagger), 마퀴, 핀 스크럽, 카운트업, 호버 확대 |
| 커뮤니티·커머스 | **중** | 카드 호버 리프트, 이미지 줌, 리스트 진입 페이드, 스켈레톤, 토스트 |
| 대시보드·백오피스 | **약(절제)** | 차트 그리기 애니메이션, 바 성장, 카운트업, 토글 전환. 장식성 모션 금지 |
| 설문·폼·챗 | **최소** | 진행바, 단계 전환, 타이핑 스트리밍, 저장 표시. 응답을 방해하지 않을 것 |

**기본 탑재 (성격 불문)**
- `IntersectionObserver` 스크롤 리빌 — `opacity+translateY`, `transition-delay`로 stagger
- 수치는 **카운트업** (`requestAnimationFrame` + easeOutCubic)
- 차트·바·게이지는 **0에서 자라나게** (`setTimeout` 60ms 후 width 지정)
- 버튼·카드 `:hover`/`:active` 상태 전부 정의. 트랜지션 `.14~.2s`
- 토스트로 피드백 (동작했다는 신호 없이 끝나는 클릭 금지)

**지금까지 쓴 기법** (재사용 가능)
틱커 무한 마퀴(hover 시 일시정지) · 히어로 패럴랙스(scroll × .18) · 홀로포일 스윕 호버 ·
캔버스 스파크라인/도넛/스택바(dpr 대응) · 발산형 막대(중립 축 정렬) · QR 스캔 라인 ·
타이핑 스트리밍 + 캐럿 · 바텀시트 슬라이드업 · 지도 핀 선택 → 시트 전환 · 로그 실시간 append

**금지**
- `transition: all` — 필요한 속성만 지정
- 레이아웃을 흔드는 애니메이션 (`width`/`top` 대신 `transform` 사용, 단 바 성장은 예외)
- 스크롤 하이재킹, 자동재생 오디오
- 모션 때문에 **첫 렌더가 늦어지는 것** — 리빌은 뷰포트 진입분만

**저장소 구조 (2026.07 정리 후)**
```
/                              루트 = https://jeonjaeman.github.io/demo-sites/
├─ index.html                  포털
├─ 404.html                    ★ 옛 경로 → 새 경로 리다이렉트 맵 (제출한 지원서 링크 보존)
├─ CLAUDE.md · README.md
├─ 데모/                       ★ 데모사이트 전부. 작업순서 번호
│  ├─ 01-운송포탈/ … 27-마케팅성과자동집계/
│  ├─ _별도레포/               개별 레포로 관리(gitignore)
│  ├─ _미구축/                 공고문만 있음(gitignore)
│  ├─ _미커밋/                 포털은 가리키나 커밋 안 됨(gitignore)
│  └─ _미배포/                 완성했으나 배포 안 하기로 한 건(gitignore)
├─ 포트폴리오/                 작업 산출물(gitignore · 배포 대상 아님)
└─ <영문별칭>/                 ASCII 별칭 — 루트 유지. 지원서에 쓰는 공개 URL
```

**새 데모를 추가할 때는 `데모/<다음번호>-<한글폴더명>/`** 에 만듭니다.

**데모 폴더 내부 구조 (고정)**
```
데모/NN-<한글폴더명>/
├─ index.html                    사용자 화면
├─ admin.html / dashboard.html   관리자 (필요 시)
├─ assets/{css,js,img}/
├─ 분석/
│  ├─ 기획_설계.md               니즈·리서치·레퍼런스·견적 근거
│  └─ 시나리오_사용자여정.md      역할별 여정·기능 존재 이유·비는 구멍
├─ 원본기획/                     (이미지 원본이 있을 때만)
├─ README.md
├─ 지원서_위시캣.txt              ★ 마크다운 아님 — 폼에 그대로 붙여넣는 순수 텍스트
└─ <원본 공고문>.txt
```

**네이밍**: 영문 브랜드명(대문자) + 한글 별칭 — TOPLOADER 탑로더 / BIZSEED 비즈시드 / BOOTHPIN 부스핀

**이미지**: Higgsfield MCP `generate_image` 사용. 실존 IP·브랜드·인물 금지, "no readable text/logos" 명시.
배너·배경용은 **720px로 리사이즈**(Pillow)해 100KB 이하로 줄입니다.

### 5단계 — 검증 (필수, 생략 불가)

```
preview_start {name} → javascript_tool로 기능 직접 호출 검증
```

체크리스트:
- [ ] 콘솔 에러 **0건** (`read_console_messages onlyErrors:true`)
- [ ] 모든 화면 렌더 에러 없음 (뷰 함수 전수 호출)
- [ ] 폼 검증이 실제로 차단하는가 (필수값 누락 시)
- [ ] 필터·검색·토글이 실제로 결과를 바꾸는가
- [ ] 다운로드가 실제 파일을 만드는가 (컬럼 수·이스케이프·BOM)
- [ ] 모바일(375px) 가로 스크롤 없음, 테이블은 자체 컨테이너 내부 스크롤
- [ ] **모션이 실제로 동작하는가** — 리빌 `.in` 부착, 카운트업 종료값, 차트 픽셀 샘플링(dpr 곱해서 조회)
- [ ] **화면 안내 문구가 실제 동작과 일치하는가** ★

★ 마지막 항목이 가장 중요합니다. 실제로 두 번 틀렸습니다:
- "직위를 교장으로 좁혀 보세요" → 전체 교장 10명이라 마스킹 안 걸림 → 문구 수정
- "한 사람이 두 등급을 동시에 만족합니다" → 해당 분포에선 미발생 → 조건부 표현으로 수정

**검증에서 발견한 버그는 숨기지 말고 보고에 포함합니다.**

### 6단계 — 문서 4종 작성

1. **분석/기획_설계.md** — 명시/숨은 니즈, 요구사항 커버리지 표, 리서치 발견 + 근거 링크, **레퍼런스 실측 표 2종(① 도메인 ② 디자인 수상작)**, 화면 구성, 제안 스택 + 일정
2. **분석/시나리오_사용자여정.md** — 등장인물, 전체 타임라인, 역할별 시나리오, 역할별 시야 차이, 기능별 존재 이유 전수 매핑, **비는 구멍(미팅 확인 필요)**
3. **README.md** — 화면 표, 체험 시나리오, ⚠️ 착수 전 확인 항목, 디자인 실측 명세, 기술 구성
4. **지원서_위시캣.txt** — **순수 텍스트**로 간결하게. 아래 작성 규칙과 견적 규칙을 따릅니다

**지원서 작성 규칙 (★ .md 아님 · .txt)**

위시캣 지원 폼은 서식이 없는 입력란입니다. 마크다운을 쓰면 `**굵게**`와 `| 표 |`가
**기호 그대로 노출**되어 오히려 읽기 나빠집니다. 그래서 지원서만 `.txt`로 씁니다.

| 금지 | 대신 |
|---|---|
| `**굵게**` · `*기울임*` · `# 제목` | 대괄호 섹션 머리 `[1] 노출 방식` |
| `\| 표 \|` 마크다운 표 | 공백 정렬 2열 (`항목` 들여쓰기 후 설명) |
| `- [링크](url)` | 이름 줄바꿈 후 URL 한 줄 그대로 |
| `> 인용` | 들여쓰기 + 평문 |

- **분량은 A4 2장 이내.** 읽는 사람은 60명 넘는 지원서를 훑습니다. 길면 안 읽힙니다
- 구조 고정: `인사 + 데모 링크` → `핵심 지적 3건` → `유사 프로젝트` → `강점` →
  `견적(공수표)` → `의뢰자 부담` → `마무리 한 문단`
- 지적은 **3건까지만.** 나머지는 "그 외 —" 한 문단으로 압축합니다
  (전체 발견은 `분석/기획_설계.md`에 있고, 지원서는 요약본입니다)
- 링크는 **ASCII 별칭**을 씁니다 (한글 경로는 폼에서 잘립니다)
- 구분선은 `────`, 목록은 `-`, 도출은 `→`, 항목 구분은 `·` 만 사용합니다
- 파일 첫 줄에 `※ 아래 본문을 지원 폼에 그대로 붙여넣으세요` 안내를 답니다
- **여러 페이지로 구성되는 사이트(브랜드 소개·기업 홈 등)는 데모가 메인 한 페이지뿐임을 명시합니다.**
  "브랜드 사이트는 보통 N페이지 내외인데, 이 데모는 검토용이라 가장 중요한 메인만 완성했고,
  실제 구축에서는 전 페이지를 이 메인과 같은 퀄리티로 디자인·개발한다(메인의 디자인 시스템을
  재사용해 하위 페이지는 더 빠르게 나온다)"를 한 단락으로 넣습니다.
  → 견적도 **데모 범위(메인)가 아니라 실제 전체 페이지 수 기준**으로 산정해 앞뒤를 맞춥니다.
  (메인만 만든 데모를 근거로 전체 사이트를 저가에 산정하면 착수 후 분쟁이 납니다.)

### 7단계 — 등록·배포

1. **포털 등록** — 루트 `index.html`의 `DEMOS` 배열 **맨 앞쪽**에 추가
   ```js
   { name:"한글명", en:"BRAND", cat:"platform|admin|home", desc:"핵심기능·차별점·디자인출처", 
     url:"./데모/NN-한글폴더/index.html", status:"live", g:"linear-gradient(...)", here:true },
   ```
2. **ASCII 별칭 폴더** — **루트에** `/영문명/index.html`을 만들어 `../데모/NN-한글폴더/`로 리다이렉트
   - 관리자 화면이 있으면 `/영문명/admin/index.html`도 생성 (`../../데모/NN-…/admin.html`)
   - **별칭이 공개 URL입니다.** 지원서에는 항상 별칭만 씁니다 — 한글 경로는 폼에서 잘립니다
   - 폴더 위치가 바뀌면 별칭의 `canonical`·`meta refresh`·`location.replace` **3곳을 모두** 고칩니다
3. **launch.json** — 포트 추가. **다음 사용 포트: 8087부터 내림차순** (8088~8099 사용 중)
4. **커밋·푸시**
   ```
   feat(폴더명): 한 줄 설명 — 레퍼런스A×B×C 디자인, /별칭 별칭
   
   Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
   ```
   - **해당 폴더 + 루트 index.html + 별칭 폴더만** staging (다른 미완성 작업 건드리지 않기)
5. **배포 확인** — Pages 200 확인 (until 루프로 대기, 한글 경로는 URL 인코딩)

**★ 폴더를 옮기거나 이름을 바꿀 때**

이 저장소는 GitHub Pages라 **폴더명이 곧 공개 URL**이고, 이미 발송한 지원서에 그 URL이 들어 있습니다.
경로를 바꾸면 반드시 아래를 함께 처리합니다.

| 대상 | 처리 |
|---|---|
| `404.html` | 옛 경로 → 새 경로 매핑 추가. 하위 경로·쿼리·해시를 이어붙여 **딥링크까지** 살립니다 |
| 별칭 폴더 | canonical · meta refresh · location.replace 3곳 |
| 루트 `index.html` | `DEMOS` 배열의 `url` |
| 사이트 내부 절대 URL | canonical·sitemap·hreflang에 `https://jeonjaeman.github.io/...`가 박힌 데모가 있습니다(글로벌투자사 168파일). **percent-encoding 형태로도** 치환해야 합니다 |
| `.claude/launch.json` | `--directory` 경로 |

옮긴 뒤에는 **루트에 서버를 띄워 옛 경로·새 경로·별칭을 전수 확인**하고 커밋합니다.

### 8단계 — 보고

사용자에게 보고할 것:
- 배포 URL (사용자/관리자)
- **리서치에서 나온 가장 중요한 발견** (이게 핵심)
- **도메인 레퍼런스 3곳**과 실측값 · **디자인 레퍼런스(수상작) 2~3곳**과 무엇을 가져왔는지
- 검증 결과 + **발견해서 고친 버그**
- 견적 요약

---

## 견적 규칙

**산정 방식 (★ 공고 예산에 맞추지 않습니다)**

- **일 50,000원** 고정 · 총액 = 일수 × 5만원 (VAT 별도)
- 기간 = 공고 명시 기간, 없으면 **기능 범위로 역산**
- **공고 예산은 참고만 합니다.** 산출액이 예산보다 낮게 나와도 **예산에 맞춰 올리지 않습니다.**
  공수대로 계산한 금액을 그대로 제시합니다
- 반대로 산출액이 예산을 넘으면 **단계 축소안**을 함께 제시합니다 (1차 범위 축소 → 2차 확장)

**금액이 낮을 때 반드시 함께 붙이는 것**

낮은 금액은 그 자체로 *"퀄리티가 낮거나 공수를 잘못 셌다"* 는 의심을 삽니다.
그 의심을 **금액을 올려서가 아니라 근거로** 없앱니다.

| 붙일 것 | 이유 |
|---|---|
| **공수표 (필수)** | "후려친 게 아니라 계산한 결과"임을 증명합니다. 작업별 일수를 전부 나열합니다 |
| **동작하는 데모** | 금액이 아니라 결과물로 판단하게 만듭니다. 이 저장소의 목적이 이것입니다 |
| **범위의 명시** | 무엇이 포함이고 무엇이 의뢰자 부담인지 선을 그어야 "싼 게 비지떡" 의심이 사라집니다 |

- **절감률을 헤드라인으로 쓰지 않습니다.** "40% 저렴합니다" ✗ → "이 범위를 이 금액에 합니다" ○
- 표에 `공고 예산 대비 −70%` 같은 문구를 **넣지 않습니다.** 스스로 공수를 의심받게 만듭니다
- 예산 차이를 **먼저 언급하지 않습니다.** 물어보면 "공수 기준으로 계산한 금액"이라고만 답합니다
- 남는 예산을 채우려고 **범위를 억지로 늘리지 않습니다.** 필요한 것만 하고 금액은 낮게 갑니다

> 참고: 클라이언트의 선정 기준 1순위는 **유사 프로젝트 경험**이고 금액은 여러 요소 중 하나입니다.
> 그러므로 금액을 낮추더라도 **포트폴리오 링크와 데모가 비어 있으면 의미가 없습니다.**
> 출처: [위시캣 뉴스센터 — 견적이 낮으면 유리할까요?](https://www.wishket.com/news-center/detail/410/)

**어필 순서 — 클라이언트 선정 기준을 그대로 따릅니다**

1. **유사 프로젝트 경험 (1순위)** — 지원서에 **포트폴리오 링크 2~3건**을 반드시 넣습니다
   - 이 저장소의 기존 데모 중 **성격이 가까운 것**을 고릅니다 (예약·결제 → SEMINAR56 / 커뮤니티 → TOPLOADER / 대시보드 → CLASSPULSE·BIZSEED / 앱 → BOOTHPIN·HANSANG)
   - 형식: `프로젝트 성격 한 줄 + 링크 + 그 건에서 해결한 문제`
   - "경력 N년" 같은 표현은 쓰지 않습니다. **결과물로 증명합니다**
2. **빠른 피드백 (2순위)** — AI 도구 활용을 **여기에 배치**합니다
   - ✗ "AI로 공수를 줄여 견적을 낮췄습니다" (가격 경쟁 프레임)
   - ○ "수정 요청을 당일 반영합니다. 화면 단위 재작업이 빠릅니다" (속도·대응력 프레임)
3. **지속 관계 (3순위)** — 오픈 후 유지보수·고도화 의향을 명시 (공고에 언급이 있으면 반드시)
4. **디자인 강점** — 웹디자이너 출신. **해당 프로젝트에서 왜 디자인이 결과를 좌우하는지**로 연결
   - 예: "설문은 이탈하면 데이터가 사라지므로 화면 완성도가 곧 데이터 품질"

**의뢰자 부담 영역** (반드시 명시)
서버·호스팅 / API 종량 과금 / 개발자 계정(Apple $99, Google $25) / 각종 신고·등록(사업자 명의) / PG 수수료 / 유료 폰트·이미지 라이선스 / 법적 판단 책임

---

## 톤 규칙

- 지적은 **단정적으로 쓰되 근거를 붙입니다.** 근거 없이 단정하지 않습니다.
- 실제 계산으로 확인되지 않은 것은 **조건부 표현**을 씁니다 ("~할 수 있습니다")
- 문제 제기 후 **반드시 대안을 제시**합니다. 지적만 하고 끝내지 않습니다.
- 의뢰자 기획의 **잘된 부분도 짚습니다** (신뢰 형성 + 실무 이해도 증명)
- 데모 데이터는 전부 가상임을 문서·화면에 명시합니다

---

## 기존 데모 참조

**두 축(도메인 + 디자인)을 모두 실측한 건** — 다음 작업의 기준입니다.

| 폴더 | 브랜드 | 별칭 | 포트 | 도메인 레퍼런스 | 디자인 레퍼런스 | 핵심 발견 |
|---|---|---|---|---|---|---|
| 마케팅성과자동집계 | ADSUM | /adsum | 8088 | Airbridge×TripleWhale×Funnel | **어워드** Dragonfly(Awwwards SOTD 26.07.22 · 7.39)×TRIONN(SOTD+DEV 26.07.27) | 광고 API에 "매출"이 없음(스마트스토어 픽셀 불가)·기여기간 차이로 채널 합산 45% 중복·전환 지연으로 1회 수집 시 34% 과소 |
| 데이터수집여성커뮤니티 | HERLOOP | /herloop | 8089 | 화해×오늘의집×커리어리 | 갤러리 Framer×Notion *(수상 미확인 — 새 기준으론 어워드 1곳을 더 채워야 합니다)* | 노출 방식(링크/발췌/전문) 법적 분기·2026.7.1 불법촬영물 이미지 확대 |
| 광융합기업지도 | LUMINA | /lumina | 8090 | 호갱노노×Airbnb | 갤러리 Linear×Stripe *(수상 미확인 — 동일)* | 과업지시서 100% vs 15일 충돌·재무 API 중소 12.3% |

※ ADSUM만 어워드 기준을 충족합니다. HERLOOP·LUMINA는 제품 사이트를 실측한 것이라
**출처 종류를 「갤러리」로 정정**했습니다. 새 건은 어워드에서 최소 1곳을 채웁니다.

**도메인 레퍼런스만 적용한 건** (디자인 레퍼런스 분리 이전 6건 + MOST)

| 폴더 | 브랜드 | 별칭 | 포트 | 도메인 레퍼런스 | 핵심 발견 |
|---|---|---|---|---|---|
| 이주민생활정보 | MOST | /most | 8091 | 서울외국인포털×Rightmove×Daft.ie | 광고/검증 분리·등록번호 필수광고정보 |
| 마케팅세미나플랫폼 | SEMINAR56 | /seminar56 | 8092 | 패스트캠퍼스×Luma×탈잉 | 56개 복제 유지보수·가상계좌 정원 충돌 |
| 행사관리플랫폼 | BOOTHPIN | /boothpin | 8093 | 당근×Airbnb×Eventbrite | 등급 규칙 계산 불가·PG 등록 |
| 교사설문시각화 | CLASSPULSE | /classpulse | 8094 | Typeform×Datawrapper×Metabase | 소규모 셀 개인 특정 |
| 연구용챗봇 | LABCHAT | /labchat | 8095 | ChatGPT×Qualtrics×Prolific | CSV 동시성 유실·모델 스냅샷 |
| 신규사업자데이터수집 | BIZSEED | /bizseed | 8096 | Apify×Airbyte×Supabase | 국세청 API 목록 조회 불가 |
| 스포츠카드통합커뮤니티 | TOPLOADER | /toploader | 8097 | Card Ladder×KREAM×너정다 | 사기방지·시세 락인 |

※ 새 건은 **디자인 레퍼런스 2~3곳을 반드시 함께** 실측하며, 그중 **최소 1곳은 어워드**에서 채웁니다.
Awwwards SOTD·CSSDA WOTD·GDWEB 선정작은 계속 바뀌므로 **작업 시점에 새로 고릅니다.**
확인 절차와 폴백 순서는 3단계 B를 따릅니다 — **"매칭되는 게 없다"는 판정은 하지 않습니다.**

---

## 하지 말 것

- 리서치 없이 데모부터 만들기
- 레퍼런스를 추측으로 쓰기 (반드시 실측)
- **디자인 레퍼런스를 건너뛰고 도메인 레퍼런스만 보기** — 그 업계 평균에 머뭅니다
- **"성격이 맞는 레퍼런스가 없다"고 판정하고 넘어가기** — 화면 유형·업종·섹션으로 내려가면
  반드시 찾을 수 있습니다. 3단계 B의 5단 폴백을 끝까지 내려갑니다
- **디자인시스템·UI/UX·인터랙션·그래픽 소스 4축 중 하나라도 비우기**
- 후보(Nominated)를 **수상작이라고 적기** · `(다수 수상)` 처럼 근거 없이 뭉뚱그리기
- 수상작의 파격을 **대시보드·백오피스에 그대로 이식하기** (모션 강도표를 따를 것)
- 수상작의 **유료·커스텀 폰트를 쓸 수 있다고 가정하기** (메트릭만 em 비율로 이식할 것)
- 색·폰트만 베끼고 **레이아웃 리듬·정보 위계·모션을 빼먹기**
- 움직임 없는 **정적 페이지로 끝내기** (요구의 일부가 "동적 사이트")
- 발견한 문제를 문서에만 적고 화면에 반영하지 않기
- 하드코딩된 수치로 "계산되는 것처럼" 보이게 하기
- 검증 없이 완료 보고하기
- **절감률을 앞세우기** ("예산 대비 −70%") — 공수 산정을 의심받습니다
- **유사 프로젝트 포트폴리오 링크 없이 지원서 쓰기** (선정 기준 1순위입니다)
- **지원서를 마크다운(.md)으로 쓰기** — 폼에 붙이면 `**`와 `|`가 그대로 보입니다. `.txt` 순수 텍스트
- **지원서에 발견 사항을 전부 나열하기** — 3건까지, A4 2장 이내. 나머지는 분석 문서에 둡니다
- AI 활용을 **가격 인하 근거**로 쓰기 (속도·대응력으로 배치할 것)
- 실존 인물·브랜드·업체를 데이터에 사용하기
- 다른 미완성 폴더를 함께 커밋하기
